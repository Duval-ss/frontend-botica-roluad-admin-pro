# Frontend Botica ROLUAD

Frontend React + Vite + Bootstrap para e-commerce de botica.

## Pasos

```bash
npm install
npm run dev
```

Backend esperado:

```txt
http://localhost:3000/api
```

Si quieres cambiarlo, crea un archivo `.env`:

```env
VITE_API_URL=http://localhost:3000/api
```
